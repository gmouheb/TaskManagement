[package]
name = "serene-interval"
version = "0.1.0"
description = "A calm Pomodoro focus timer."
authors = ["Serene Interval"]
edition = "2021"

[lib]
name = "serene_interval_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.2", features = [] }

[dependencies]
tauri = { version = "2.11.2", features = [] }
